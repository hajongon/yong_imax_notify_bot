# 2026-03-06 로그인 팝업 처리 및 수동 로그인 대기 반영

## 요구사항
- 시간대(예: 13:20) 클릭 시 로그인 팝업이 뜨면,
  - 팝업의 `확인` 버튼을 자동 클릭
  - 사용자가 수동 로그인 완료할 때까지 대기
  - 이후 바로 좌석 체크 로직 진행

## 변경 파일
- `test.py`

## 반영 내용
1. 로그인 팝업 감지/처리 함수 추가
- 함수: `handle_login_popup_and_wait()`
- 동작:
  - 짧은 대기(`LOGIN_POPUP_WAIT_SEC=2`)로 `확인` 버튼 탐색
  - 발견 시 클릭
  - 콘솔에 안내 출력 후 `input()`으로 사용자 Enter 대기
  - 표준입력이 없으면 15초 대기 폴백

2. 시간대 선택 함수에 연동
- 함수: `click_time_slot(start_time)`
- 동작 변경:
  - 시간대 버튼 클릭 직후 `handle_login_popup_and_wait()` 실행
  - 로그인 팝업을 처리했다면, 로그인 완료 후 같은 시간대를 재선택
  - 이후 좌석/활성 시간대 로딩 대기 후 복귀

## 기대 동작
1. `13:20`(또는 `16:30`) 클릭
2. 로그인 팝업 발생 시 `확인` 자동 클릭
3. 사용자 수동 로그인
4. 터미널 Enter 입력
5. 동일 시간대 재선택 후 좌석 체크 진행
